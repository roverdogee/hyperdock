# Finder
.DS_Store

# Build intermediates and products — regenerated by scripts/build.sh
.build/
build/
DerivedData/
.derived/

# Release artifacts — produced by scripts/release.sh, not source
dist/

# Per-user Xcode state
*.xcuserdatad/
xcuserdata/

# Generated by XcodeGen from project.yml on every build (scripts/build.sh)
HyperDock.xcodeproj/

# The original preference pane and binaries remain local research references. Extracted
# artwork under Sources/HyperDock/Resources/OriginalHyperDock is tracked separately with
# an explicit copyright notice for compatibility themes.
ReferenceAssets-PreferencePane/
